spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/flash_sale?options=-c%20TimeZone=Asia/Kolkata
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

